# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

def testing_pods
    pod 'Quick'
    pod 'Nimble'
end

target 'SwiftRepoViewer' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SwiftRepoViewer

  # Pods for Swift Repo Viewer
  pod 'Alamofire', '~> 5.2'
  pod 'Moya', '~> 14.0'
  pod 'SDWebImage'
  testing_pods
  
  target 'SwiftRepoViewerTests' do
    inherit! :search_paths
    # Pods for testing
  end
  
  target 'SwiftRepoViewerUITests' do
    # Pods for testing
  end

end
